[tox]
skipsdist = true

[testenv]
basepython = python3

[testenv:style]
deps =
    flake8
    black
changedir = {toxinidir}
commands =
    black fabfile.py manage.py wsgi.py accounts activities audit conf contacts deals invoices logbook offers projects services stuff tools workbench
    flake8 .
skip_install = true
